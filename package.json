{
  "name": "agent-relay",
  "version": "2.6.0",
  "description": "Local multi-agent coding coordinator for Claude Code, Codex, GitHub Copilot, and custom CLI models.",
  "type": "module",
  "private": false,
  "scripts": {
    "start": "node relay/server.mjs",
    "check": "node --check relay/server.mjs && node --check relay/config.mjs && node --check relay/run-store.mjs && node --check relay/review-gate.mjs && node --test",
    "test": "node --test"
  },
  "engines": { "node": ">=22" },
  "license": "MIT",
  "keywords": ["ai", "coding-agent", "claude-code", "codex", "github-copilot", "multi-agent"]
}
